dist: trusty
sudo: required
language: node_js
node_js:
  - node

before_script:
  - npm i -g cordova ionic
  - mkdir www

script:
  - npm run lint
  - npm run lint:css
  - cordova prepare
  - npm run build
